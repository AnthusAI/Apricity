{
  "id": "apricitus-5cb89363-495b-4321-8301-e08c43536bda",
  "title": "Browser audio: failures hang or become permanent instead of surfacing as errors",
  "description": "In `web/src/audio/`:\n- Render workers get no `onerror` or `onmessageerror` handler (`player.ts:82-90`). A wasm trap in a worker leaves the `waiting` promise and the `source-ready` waits pending forever.\n- In the worklet, `instantiate(...).then(...)` has no catch (`engine-worklet.ts:31`), so `engineReady` never resolves on failure.\n- `loadSources` stores each decode promise in `this.decoding` and never removes it on failure, so a failed decode can never be retried without a reload. Compare `peaks.ts:50` and `apricity.ts:126`, which delete the cache entry on rejection.\n- `init()` caches `this.ready` (`player.ts:60`) even when it rejects, so a failed `addModule` or wasm fetch is permanent.\n\nDone when: every worker and worklet failure rejects the waiting promise with a message, failed decode and init promises are evicted from their caches, and the play button shows the error. A test drives a worker error and asserts the player reports it and can recover.",
  "type": "bug",
  "status": "open",
  "priority": 1,
  "assignee": null,
  "creator": null,
  "parent": "apricitus-de033deb-4fc0-4768-95a7-8ba46603b71b",
  "labels": [],
  "dependencies": [],
  "comments": [],
  "created_at": "2026-09-26T05:32:12.961115Z",
  "updated_at": "2026-09-26T05:32:12.961288Z",
  "closed_at": null,
  "right_now_summary": null,
  "right_now_updated_at": null,
  "custom": {},
  "agent": {
    "platform": "claude_code",
    "model": "Claude",
    "name": "Project improvement audit",
    "settings": {}
  },
  "agent_assignment": null
}