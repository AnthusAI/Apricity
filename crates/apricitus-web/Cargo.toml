[package]
name = "apricitus-web"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "C-ABI WebAssembly surface of Apricitus for the browser: compiler (page), renderer (worker), mixer (AudioWorklet)."

[lib]
crate-type = ["cdylib"]

[dependencies]
apricitus-engine.workspace = true
apricitus-score.workspace = true
serde.workspace = true
serde_json.workspace = true
serde_yaml = "0.9"
